apiVersion: v1
kind: ConfigMap
metadata:
  name: db-data-param
  labels:
    app: pdb
data:
  POSTGRES_DB: testDatabase
  POSTGRES_USER: testUser
  POSTGRES_PASSWORD: 
  PGDATA: /var/lib/postgresql/data/pgdata